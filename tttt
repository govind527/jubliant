this is file demo


Parameterizing Inference Year and Vintage Month While debugging, the discussion shifts to the code logic, specifically how the inference_year and vintage_month are dynamically calculated
. A speaker points out that the current logic extracts the year "2025" directly from the joblib file string using an inference_year - 1 calculation
. They warn that this hardcoded extraction might cause issues if they ever want to train models on historical past years (like 2024)
. They debate whether to extract the year from the maximum month ID of the transformed dataset or just use a current_year - 1 logic
. Ultimately, the team agrees that since they will primarily be training on future data rather than past years, extracting the test year directly from the included_months parameter will work fine for their needs
. They also note that when the training pipeline is eventually automated, it will likely be set to train data up to two months prior to the current month (current_month - 2)
