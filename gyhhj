import mlflow
import time, json, uuid
from datetime import datetime

def predict_with_mlflow_logging(model, df):
    request_id = str(uuid.uuid4())
    user = spark.sql("select current_user()").collect()[0][0]

    with mlflow.start_run(run_name=f"inference_{request_id}", nested=True):
        start = time.time()
        preds = model.predict(df)
        end = time.time()

        # Log metadata
        mlflow.set_tag("request_id", request_id)
        mlflow.set_tag("requested_by", user)

        # Log metrics
        mlflow.log_metric("latency_ms", int((end - start) * 1000))
        mlflow.log_metric("rows", len(df))

        # Log payloads as artifacts
        mlflow.log_text(df.to_json(), "input.json")
        mlflow.log_text(json.dumps(preds.tolist()), "output.json")

    return preds


model = mlflow.sklearn.load_model(
    "models:/finacopilot.ml_model.revenue_forecasting/1"
)

preds = predict_with_mlflow_logging(model, df)




 For now, don't remove that much apart from those forecast columns. Don't remove, we will do that. Okay? Later maybe while fine-tuning we might need it. So for now let's do this, set up everything first. Yes, in a way... meaning... Everything, yes, yes, yes. Yes, yes. Make your script according to this data. Test everything. Then after this, the future processes... meaning one is to make folders. And one thing. This incremental data that will come... I showed you the '24 Excel file, it will come according to that. Correct. Hmm. So according to that also, have to set up a dummy file. According to that, have to set up an incremental dummy file. According to that, have to set up a historical dummy file. Plus look, there will be two categories here. One Raw Data, one Transformed Data. Okay? We will have to set up incremental plus historical for both these categories. Basically, there will be four tables.
[Speaker1]: Hmm, okay.
[Speaker2]: So we... I am telling you Raw Data Historical because... look, suppose we ever need to check '23 data, was there any issue... so that we can refer back and you know, we can make some changes. That is why I was thinking of Raw Data also, that we will... meaning last two years or three years we will store. You understand?
[Speaker2 ]: Yes.
[speaker 1]: Because our model will also retrain, right? So we need to store that '23, '24 and whatever historical data is there. That's what I was thinking.
[Speaker2]: There is one more thing. Let me show one thing. Right now in between... meaning after training also we are storing data. By the way. Hmm. There is a catalog, but right now it is also dummy in a way. Consider it like that. Okay. Like here we come. Like what you are saying... meaning that same thing will be followed. Show me first. Okay? These four tables are set up. Like this is Test Feature, right? One is Train Feature. So what is happening is that Test Feature is ours in a way... that same one. Meaning '25 one. Sorry, Test Feature is '25 one. Train is ours '23 to... Full Data is ours in a way... all of it. In a way, I will show you. Just for... meaning testing purpose, we did it according to our thinking.
[Speaker2]: Ah, you want to store it separate... Train and Test Feature data.
[Speaker1]: Yes.
[Speaker1]: But what you said is also actually correct because we... meaning how incremental...
[Speaker2]: What I was thinking, right... we... In blob... one table which is historicals, that will be one table. Okay. But there will be one folder in the blob where incremental data will always come. So we will basically put a Change Data Capture trigger on that.
[Speaker1]: Oh, okay. Meaning that in a way... however anything changes in the data, then triggering that... yes... that new data has come.
[Speaker2]: Right.



Here is a detailed breakdown of what speaker is trying to convey, categorized by topic:

### 1. Standardization of Code Structure
Rucha is critical of the current method where all code (training, inference, logging) is crammed into a single Databricks notebook.
*   **Modularization:** She insists that the team needs to move away from using a single, resource-intensive notebook that is "lagging",.
*   **The `.py` Approach:** She instructs Govind to create separate Python (`.py`) scripts for functions and classes. These scripts should then be imported into a notebook to run the pipeline, rather than writing all code directly in the notebook cells,.
*   **Folder Structure:** She agrees that they need to follow a standard folder structure (separating config, training, and artifacts), similar to previous projects, rather than the current ad-hoc setup,.

### 2. The "Cut-Off Month" and Imputation Logic
A major part of her explanation focuses on how the forecasting model handles future predictions.
*   **Synthetic Rows:** She explains that for the model to predict revenue for the remainder of a year, the system must create "imputed" or "synthetic" rows for future months based on the data available up to a "cut-off month",.
*   **The Workflow:**
    1.  Raw data arrives and triggers the pipeline.
    2.  The new data is transformed and appended.
    3.  Based on this incremental data, the system creates imputed rows for the rest of the year (e.g., if data comes for June, rows are generated for July–December).
    4.  The model then runs inference on these specific imputed rows.

### 3. Data Integrity and Feature Engineering
Rucha is correcting misconceptions regarding the data used for testing versus production.
*   **Raw vs. Transformed Data:** She clarifies that the data arriving in the system is "Raw Data" (e.g., Committed Signed/Unsigned columns). This data needs to be "Transformed" (grouped and aggregated) before the model can use it. She notes that the current testing was done on data that was already transformed, which is different from the real-time scenario,.
*   **Task Assignment:** She decides that she will write the **Feature Transformation Script** herself to bridge the gap between Steps 2 and 3 (Data Ingestion and Feature Creation),.

### 4. Storage and Trigger Strategy
Rucha proposes a specific architecture for how data should be stored and triggered in the production environment.
*   **Four-Table Structure:** She suggests setting up four specific storage tables/locations to manage data history and updates:
    1.  Raw Data (Historical)
    2.  Raw Data (Incremental)
    3.  Transformed Data (Historical)
    4.  Transformed Data (Incremental).
*   **Reasoning:** She argues that keeping historical raw data is essential for debugging ("if we need to check '23 data... refer back") and for future model retraining,.
*   **Change Data Capture (CDC):** She proposes using a CDC trigger on the Blob storage. This means the pipeline will automatically start (trigger) whenever a new incremental data file lands in the specific folder.

### 5. Specific Testing Targets for the Week
Finally, Rucha defines the immediate goals for Govind to stabilize the model:
*   **Training Data:** Train the model using data from 2023, 2024.
*   **Testing Scenario:** Set the "cut-off month" to June 2025 and test the generation of synthetic rows and predictions for the rest of the year use june test data to predict and generate the remaining month using imputation logic,.
*   **Pipeline Scope:** She clarifies that "Training/Retraining" is a periodic event (once every 6 months), whereas "Feature Creation" and "Inference" are monthly tasks. The pipeline currently needs to focus on the monthly inference flow,.

you have to do two things. What I understand... is update the config. Okay? And then write the Conditional Model Path Rendering logic. Have to do both these things.
[Speaker 2]: Did you write that path rendering logic or...
[Speaker 1]: Path rendering one is nothing. Just have to do like this.
[Speaker 2]: By applying If-Else... that meaning this...


Data Quality and "Analyzer"
Speaker: Okay, seen action. Okay, third thing is file mismatch. Okay, file miss... Now we are expecting certain columns, certain formats. Okay , we have discussed this before, that there could be 100 columns but we will need only 20 columns. So are we checking that these 20 columns are present or not? speaker: Yes, we are checking. Yes, while transforming. If it's not there, you end the pipe, right? No? Okay, fine. Speaker: Now if these columns are null or something... do we have any minimum number of null values? "If nulls are this minimum amount..." then that is a... we hadn't discussed something like this, but have you done anything? Team Member: Suppose 80%... suppose we doing that back... Speaker: If there is only 80% data, and 20% or more is missing... 100% data quality is not good. We get garbage output. So for that, we will see: file input date, column store signature, the health of the input data. Team Member: Okay, we do this regarding how many... by doing info or describe on the dataframe... Speaker: Data analyzer... so how many rows are there? How many columns? How many values? Okay. And one thing... [check] the ones we need, but not other columns... If it's not matching, then we can tell that the data quality is not good. Correct? Add that to the last. Govind, can I do it? The data analyze part. Govind: Okay. Speaker: Yes, meaning we have to check everything once... like, are our columns correct? How many null values are inside? Or what is the percentage? As a checkpoint. Give a sample test too. Team Member: Okay, fine. And one more thing, Prateek himself told me that for any rows where the entire forecast column is coming as zero, drop those rows. So that is being handled. Speaker: Okay so input data... be like another weapon to kind of... if data prediction is not good, okay... Then we can tell the input data itself is not good. Team Member: There are like 36,000 to 50,000 rows there... And their ordering is also not in a sequence. So doing backfill or forward fill is also kind of, you know, risky. In the pipeline, we filter out the columns we need and we have to put that in a proper order.
, let's make a separate script/theme. Okay, for data quality... As soon as you read the data, we will skip [checking] what the header is, we will see the data... what do you call it... data frame shape description... those things. This analysis first, save that and add me to the log. And one good thing is it can also be shown in the... 
